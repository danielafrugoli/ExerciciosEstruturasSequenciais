Algoritmo "problema_circulo"

Var
    raio_Circulo, area : real


Inicio

      escreval ("Digite o valor do raio do circulo: ")
      leia (raio_Circulo)
      
      area <- Pi * (Exp(raio_Circulo,2))
      
      escreval ("Area = ", area)


Fimalgoritmo